# サーバーAPIのベースURL(未設定時は https://1m26-18.trap.show/api/v1)
# ローカルサーバーで試す場合の例: VITE_API_BASE=http://localhost:8080/api/v1
VITE_API_BASE=http://localhost:8080/api/v1
# WebTransportサーバーのホスト名(未設定時は VITE_API_BASE から導出)
VITE_WEBTRANSPORT_HOST=localhost
# スマホ実機から参加する場合の注意:
# 上記が localhost のままでも、controller を LAN IP で開いていれば
# 実行時に自動でページのホスト名に置き換える。明示指定する場合は
# PC の LAN IP を書く 例: VITE_API_BASE=http://192.168.1.5:8080/api/v1
# 例: VITE_WEBTRANSPORT_HOST=192.168.1.5
