{
  "name": "1m26s_18_client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n viewer,controller -c blue,magenta \"npm run dev -w apps/viewer\" \"npm run dev -w apps/controller\"",
    "dev:viewer": "npm run dev -w apps/viewer",
    "dev:controller": "npm run dev -w apps/controller",
    "build": "npm run build -w apps/viewer && npm run build -w apps/controller",
    "lint": "npm run lint -w apps/viewer && npm run lint -w apps/controller",
    "preview": "node scripts/preview.mjs",
    "stamps:fetch": "node scripts/stamps.mjs fetch",
    "stamps": "node scripts/stamps.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.55.0"
  }
}
